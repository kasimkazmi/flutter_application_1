import 'package:flutter/cupertino.dart';

class DestinationsContent extends StatelessWidget {
  const DestinationsContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Destinations Content'),
    );
  }
}
